import Image from "next/image";

export default function Home() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 font-sans dark:bg-black">
     At start go with good texh stack dont waste much time on it.
     later on when the product get on scale you can choose whatever stack you need on the basis of scale.
    </div>
  );
}
